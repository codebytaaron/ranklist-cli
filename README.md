# ranklist-cli

A tiny CLI tool that converts ranking lists into clean JSON or CSV.

It handles:
- TSV (tabs)
- CSV
- Markdown tables
- Aligned text with 2+ spaces between columns

## Install
```bash
npm install
npm link
